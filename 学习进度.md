# Python 学习进度

> 这份文件用于记录每天的学习成果、项目状态、问题和下一步。  
> 后续继续学习时，优先以本文件和项目代码为准。

## 当前状态

- **记录日期**：2026-09-15
- **当前阶段**：第 1 周
- **Day 1**：已完成
- **Day 2**：进行中
- **主线练习项目**：Todo CLI 待办事项工具
- **项目目录**：`day1/todo_app/`
- **GitHub 仓库**：<https://github.com/pponak/Todo-app>
- **当前分支**：`main`

## Day 1 总结：建立第一个 Python 项目

### 已完成

- 使用 VS Code 打开并管理项目；
- 创建 `day1/todo_app/` 项目目录；
- 创建 Python 虚拟环境 `.venv`；
- 在 VS Code 中选择当前项目的 Python 解释器；
- 使用 VS Code 集成终端运行 Python 程序；
- 编写能够重复显示、接收选择并正常退出的命令行菜单；
- 编写 `README.md`，记录项目用途、当前功能和运行方式；
- 编写 `.gitignore`，忽略 `.venv/`、`__pycache__/` 和 `*.pyc`；
- 初始化本地 Git 仓库；
- 设置当前项目的 Git 用户名和邮箱；
- 完成第一次 Git 提交；
- 将本地 `main` 分支上传到 GitHub。

### Day 1 的项目文件

```text
todo_app/
├── .venv/
├── .gitignore
├── README.md
├── main.py
└── 学习进度.md
```

### 第一次提交

- **提交信息**：`完成Todo CLI基础菜单`
- **提交编号**：`9ce6a7b`
- **提交内容**：`.gitignore`、`README.md`、`main.py`

### 今天学到的概念

- Python 项目不只是一个 `.py` 文件，还包括说明文件、依赖环境和版本记录；
- 虚拟环境用于隔离不同项目的 Python 依赖；
- `.gitignore` 只是不让 Git 跟踪指定文件，不会删除电脑上的文件；
- `git init` 创建本地仓库；
- `git add` 将文件放入待提交区；
- `git commit` 保存一个本地版本；
- `git push` 才会把本地版本上传到 GitHub；
- `while True`、`input()`、`if/elif/else` 和 `break` 可以组成一个基础命令行菜单。

### VS Code 技巧

| 快捷键 | 作用 |
| --- | --- |
| `Ctrl + S` | 保存文件 |
| `Ctrl + P` | 快速打开文件 |
| `Ctrl + Shift + P` | 打开命令面板 |
| ``Ctrl + ` `` | 显示或隐藏集成终端 |
| `Ctrl + /` | 注释或取消注释 |

## 遇到的问题及解决方式

### 1. PowerShell 命令在 cmd 中执行

曾在 `cmd` 终端中执行 PowerShell 的 `.ps1` 激活命令，Windows 弹出了“选择一个应用以打开此 .ps1 文件”。

解决方式：改用适合 `cmd` 的命令：

```cmd
.venv\Scripts\activate.bat
```

### 2. `.gitignore` 拼写问题

`__pycache__/` 少写了一个下划线，已经修正。

### 3. `elif` 结构重复导致缩进错误

保留了旧的 `elif choice in [...]`，导致新分支前没有代码块。删除旧分支后，代码已通过语法检查。

## Day 2 当前进度

已经实现“添加任务”和“查看任务”功能，并完成了基础持久化：

- 在 `main.py` 中创建了 `tasks = []`；
- 用户输入的任务通过 `tasks.append(task)` 加入列表；
- “添加任务”分支已经写入代码；
- “查看任务”分支已经写入代码；
- 使用 `for` 循环遍历任务列表；
- 使用 `enumerate(tasks, start=1)` 为任务生成从 1 开始的编号；
- 空列表和多个任务都已经测试通过；
- 使用 `tasks.txt` 保存任务，每行保存一个任务；
- 使用 `open()`、`with` 和 UTF-8 编码读取、写入文件；
- 使用 `load_tasks()` 在程序启动时读取任务；
- 使用 `save_tasks()` 在添加任务后保存任务；
- 使用 `FileNotFoundError` 处理第一次运行时文件不存在的情况；
- 已验证关闭程序后重新启动，任务仍然存在；
- `tasks.txt` 已加入 `.gitignore`，不会把本机任务数据提交到 Git。

当前核心代码行为：

```text
选择 1
  ↓
输入任务
  ↓
加入 tasks 列表
  ↓
显示“任务已添加”

选择 2
  ↓
使用 for 循环遍历 tasks
  ↓
使用 enumerate() 显示任务编号和内容

程序启动
  ↓
从 tasks.txt 读取任务
  ↓
添加任务后重新写入 tasks.txt
```

## 下次从哪里开始

本次已完成持久化基础，下次继续 Day 2，顺序如下：

1. 设计能够保存“是否完成”的任务数据结构；
2. 实现“完成任务”功能；
3. 实现“删除任务”功能。

下次的第一个具体动作是：

```text
思考：除了任务文字，还需要保存哪些信息，才能知道任务是否完成？
```

## 阶段进度

- [x] 创建项目目录
- [x] 创建并启用虚拟环境
- [x] 使用 VS Code 编写和运行程序
- [x] 完成基础命令行菜单
- [x] 创建 README 和 `.gitignore`
- [x] 完成第一次 Git 提交
- [x] 上传到 GitHub
- [x] Day 2：完成添加和查看任务
- [ ] 第 1 周：完成 Todo CLI 基础版本
