# Python 学习进度

> 这份文件用于记录每天的学习成果、项目状态、问题和下一步。  
> 后续继续学习时，优先以本文件和项目代码为准。

## 当前状态

- **记录日期**：2026-09-15
- **当前阶段**：第 1 周
- **Day 1**：已完成
- **Day 2**：进行中
- **主线练习项目**：Todo CLI 待办事项工具
- **项目目录**：`day1/todo_app/`
- **GitHub 仓库**：<https://github.com/pponak/Todo-app>
- **当前分支**：`main`

## 学习方式偏好

- 以学习和理解为主，不要由助手直接代改项目文件；
- 先讲清一个小概念，再由本人修改代码；
- 修改后由助手只读检查、解释问题并协助测试；
- 不要未经明确同意更新日志、提交 Git 或进行其他文件操作；
- 注释可以使用中文，重点解释代码的作用和原因；
- 任务完成状态使用 `[√]`，未完成状态使用 `[ ]`。

## Day 1 总结：建立第一个 Python 项目

### 已完成

- 使用 VS Code 打开并管理项目；
- 创建 `day1/todo_app/` 项目目录；
- 创建 Python 虚拟环境 `.venv`；
- 在 VS Code 中选择当前项目的 Python 解释器；
- 使用 VS Code 集成终端运行 Python 程序；
- 编写能够重复显示、接收选择并正常退出的命令行菜单；
- 编写 `README.md`，记录项目用途、当前功能和运行方式；
- 编写 `.gitignore`，忽略 `.venv/`、`__pycache__/` 和 `*.pyc`；
- 初始化本地 Git 仓库；
- 设置当前项目的 Git 用户名和邮箱；
- 完成第一次 Git 提交；
- 将本地 `main` 分支上传到 GitHub。

### Day 1 的项目文件

```text
todo_app/
├── .venv/
├── .gitignore
├── README.md
├── main.py
└── 学习进度.md
```

### 第一次提交

- **提交信息**：`完成Todo CLI基础菜单`
- **提交编号**：`9ce6a7b`
- **提交内容**：`.gitignore`、`README.md`、`main.py`

### 今天学到的概念

- Python 项目不只是一个 `.py` 文件，还包括说明文件、依赖环境和版本记录；
- 虚拟环境用于隔离不同项目的 Python 依赖；
- `.gitignore` 只是不让 Git 跟踪指定文件，不会删除电脑上的文件；
- `git init` 创建本地仓库；
- `git add` 将文件放入待提交区；
- `git commit` 保存一个本地版本；
- `git push` 才会把本地版本上传到 GitHub；
- `while True`、`input()`、`if/elif/else` 和 `break` 可以组成一个基础命令行菜单。

### VS Code 技巧

| 快捷键 | 作用 |
| --- | --- |
| `Ctrl + S` | 保存文件 |
| `Ctrl + P` | 快速打开文件 |
| `Ctrl + Shift + P` | 打开命令面板 |
| ``Ctrl + ` `` | 显示或隐藏集成终端 |
| `Ctrl + /` | 注释或取消注释 |

## 遇到的问题及解决方式

### 1. PowerShell 命令在 cmd 中执行

曾在 `cmd` 终端中执行 PowerShell 的 `.ps1` 激活命令，Windows 弹出了“选择一个应用以打开此 .ps1 文件”。

解决方式：改用适合 `cmd` 的命令：

```cmd
.venv\Scripts\activate.bat
```

### 2. `.gitignore` 拼写问题

`__pycache__/` 少写了一个下划线，已经修正。

### 3. `elif` 结构重复导致缩进错误

保留了旧的 `elif choice in [...]`，导致新分支前没有代码块。删除旧分支后，代码已通过语法检查。

## Day 2 当前进度

已经完成 Todo CLI 的主要基础功能：

- 使用 `tasks` 列表保存任务；
- 使用字典保存任务的标题和完成状态：`{"title": "...", "done": False}`；
- 使用 `for` 循环和 `enumerate()` 查看并编号任务；
- 使用 `json.load()` 从 `tasks.json` 读取任务；
- 使用 `json.dump()` 将任务保存到 `tasks.json`；
- 使用 `save_tasks(tasks)` 函数集中处理保存逻辑；
- 使用 `load_tasks()` 函数集中处理读取逻辑；
- 实现了添加、查看、完成和删除任务；
- 完成状态使用 `[√]`，未完成状态使用 `[ ]`；
- 使用 `try/except`、`ValueError`、`len()` 和 `continue` 处理部分无效输入；
- 添加、完成、删除和重新启动读取都已经测试通过。

当前核心代码行为：

```text
选择 1
  ↓
输入任务
  ↓
加入 tasks 列表
  ↓
显示“任务已添加”

选择 2
  ↓
使用 for 循环遍历 tasks
 ↓
使用 enumerate() 显示编号、状态和任务内容

选择 3
  ↓
将用户编号转换为列表索引
  ↓
修改任务的 done 状态
  ↓
保存任务列表

选择 4
  ↓
将用户编号转换为列表索引
  ↓
使用 pop() 删除任务
  ↓
保存剩余任务
```

## 下次从哪里开始




## 今天学到的概念

- `try/except`：捕获可能出现的错误；
- `with open(...)`：打开文件，并在使用完后自动关闭；
- `"r"` 和 `"w"`：分别表示读取模式和写入模式；
- `json.load()`：把 JSON 文件转换为 Python 数据；
- `json.dump()`：把 Python 数据转换为 JSON 并写入文件；
- 列表中的字典：让一个任务同时保存标题和完成状态；
- `True` 和 `False`：Python 中的布尔值；
- `pop(index)`：删除指定位置的列表元素，并返回被删除的元素；
- `len(tasks)`：获取任务数量；
- `continue`：结束本次循环并回到菜单；
- 函数的定义、参数、调用和 `return`；
- 用函数减少重复代码。

## 今天的学习结果

- Day 2 的 Todo CLI 核心功能已经完成；
- 添加、查看、完成、删除和重启读取都已经测试通过；
- `tasks.json` 是当前使用的数据文件；
- 旧的 `tasks.txt` 仍保留在项目中，作为旧格式数据，不要擅自删除。
- 已创建 Git 提交：`153652c`（`完成day2学习，Todo CLI核心功能`）；
- `tasks.json` 和 `tasks.txt` 是本地数据文件，未提交；
- 本次学习日志补充尚未再次提交 Git；

## 下次从哪里开始

下次继续函数整理，不重复今天已经完成的内容：

1. 添加并检查 `get_task_index(tasks, action)` 函数；
2. 用这个函数替换“完成任务”和“删除任务”中的重复输入检查代码；
3. 学习为什么要用 `is None` 判断函数返回值；
4. 重新测试添加、查看、完成、删除和重启读取；
5. 如果本人明确同意，再由本人决定是否更新日志和提交 Git。

下次的第一个具体动作是：

```text
在 load_tasks() 下面添加 get_task_index() 函数，暂时不删除旧的完成/删除代码。
```

## 阶段进度

- [x] 创建项目目录
- [x] 创建并启用虚拟环境
- [x] 使用 VS Code 编写和运行程序
- [x] 完成基础命令行菜单
- [x] 创建 README 和 `.gitignore`
- [x] 完成第一次 Git 提交
- [x] 上传到 GitHub
- [x] Day 2：完成添加和查看任务
- [x] Day 2：完成任务、删除任务和 JSON 持久化
- [ ] 第 1 周：完成 Todo CLI 基础版本
